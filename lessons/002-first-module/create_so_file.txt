
#mac için
clang -shared -undefined dynamic_lookup $(python3-config --cflags --ldflags) -o umit_core.so umit_core.c



#linux için
gcc -shared -fPIC $(python3-config --cflags --ldflags) -o umit_core.so umit_core.c


#windows için bir py dosyası oluştur aşağıdakileri içine yaz ve çalıştır 😉

from setuptools import setup, Extension
setup(
    name="umit_core",
    version="1.0",
    ext_modules=[Extension("umit_core", ["umit_core.c"])]
)
